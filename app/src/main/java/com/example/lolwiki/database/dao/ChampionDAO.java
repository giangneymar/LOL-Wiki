package com.example.lolwiki.database.dao;

import androidx.room.Dao;
import androidx.room.Query;

@Dao
public interface ChampionDAO {
   // @Query("SELECT * FROM `champion` WHERE tier = :s ORDER BY `name` ASC");
}
